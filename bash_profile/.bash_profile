#
# ~/.bash_profile - login shell (executed by bash(1) for login shells)
#

# Load interactive shell config (aliases, functions, tools, PATH)
[[ -f ~/.bashrc ]] && . ~/.bashrc

# Personal aliases (interactive)
alias ls='ls --color=auto'
alias vi='nvim'
